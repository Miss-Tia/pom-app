<h1><%= @plant.name %></h1>

<p><strong>Notes:</strong> <%= @plant.notes %></p>
<% if @plant.days_to_harvest.present? %>
  <p><strong>Days to Harvest:</strong> <%= @plant.days_to_harvest %> days</p>
<% end %>

<div class="entry-actions">
  <%= link_to "✏️ Edit Plant", edit_plant_path(@plant), class: "btn green-btn" %>
  <%= link_to "🗑️ Delete Plant", plant_path(@plant), method: :delete, data: { confirm: "Are you sure?" }, class: "btn danger-btn btn-small" %>
  <%= link_to "⬅️ Back", request.referer || dashboard_path(current_user), class: "btn brown-btn btn-small" %>
</div>
